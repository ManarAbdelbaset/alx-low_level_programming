#include "main.h"

/**
 * _isalpha - checks for alphabetic character
 * @C: The character to be cheeked
 * Return: 1 for alphabetic character or 0 for anything else
*/

int _isalpha(int c)
{
	if ((c >= 97 && c <= 122)
			return (1);
			if (c >= 65 && c <= 90)
			return (1);
			return (0);
}
